# AI-Pathfinding-Agent
finds the optimal route in a maze using AI search algorithms like BFS, DFS, and A*.
A grid-based maze where an agent (AI) finds a path from the start node (S) to the goal node (G) using:

Breadth-First Search (BFS)

Depth-First Search (DFS)

A Algorithm (using heuristic)*
